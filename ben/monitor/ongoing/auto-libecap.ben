title = "libecap (auto)";
is_affected = .depends ~ /libecap3|libecap3-dev|libecap2|libecap2-dev/;
is_good = .depends ~ /libecap3|libecap3-dev/;
is_bad = .depends ~ /libecap2|libecap2-dev/;
notes = "This tracker was setup by a very simple automated tool.  The tool may not be very smart...

Extra information (collected entirely from testing!):
 * can-smooth-update: maybe (ignoring rdep-less binaries)";
