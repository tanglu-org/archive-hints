title = "rootstrap-rm (auto)";
is_affected = .depends ~ /\b(rootstrap)\b/;
is_good = false;
is_bad = .depends ~ /\b(rootstrap)\b/;
notes = "This tracker was setup by a very simple automated tool.  The tool may not be very smart...

Extra information (collected entirely from testing!):
 * can-smooth-update: no - rootstrap is not in libs or oldlibs";
